# VideoScrollSection 组件测试指南

## 测试页面

我们创建了一个专门的测试页面来验证 VideoScrollSection 组件的功能。

### 访问测试页面

```bash
# 启动开发服务器
npm run dev

# 访问测试页面
http://localhost:4321/test-video-scroll
```

## 测试页面特性

### 1. 完整的 Lenis 集成
- ✅ Lenis 平滑滚动已启用
- ✅ Lenis 实例暴露到 `window.lenis`
- ✅ RAF 循环正常运行
- ✅ 与 GSAP ScrollTrigger 同步

### 2. 调试模式开启
- ✅ `showDebug={true}` - 显示实时调试信息
- ✅ 状态显示（进入区域、播放中、离开区域）
- ✅ 进度显示（0% - 100%）
- ✅ 时间显示（当前时间 / 总时长）

### 3. 详细的测试环境信息
页面显示了所有配置参数：
- Lenis 平滑滚动状态
- GSAP ScrollTrigger 集成状态
- 视频源路径
- 滚动距离（3x 视口高度）
- Scrub 速度（0.5）
- 调试模式状态

## 测试步骤

### 步骤 1: 启动开发服务器

```bash
npm run dev
```

### 步骤 2: 打开测试页面

在浏览器中访问：
```
http://localhost:4321/test-video-scroll
```

### 步骤 3: 打开开发者工具

按 `F12` 或右键 → 检查，打开浏览器开发者工具，切换到 Console（控制台）标签。

### 步骤 4: 检查初始化日志

你应该在控制台看到以下日志（按顺序）：

```
🚀 [Test Page] 初始化 Lenis 平滑滚动...
✅ [Test Page] Lenis 实例已暴露到 window.lenis
✅ [Test Page] Lenis RAF 循环已启动
🎬 初始化视频滚动组件...
🔍 [Lenis Detection] Lenis 实例已检测到
🔗 [Lenis Sync] 开始配置 Lenis 与 GSAP ScrollTrigger 同步...
🔗 [Lenis Sync] ✓ 已绑定滚动事件
🔗 [Lenis Sync] ✓ 已禁用延迟平滑
✅ [Lenis Sync] Lenis 同步配置完成
✅ 找到视频元素
✅ 视频准备就绪，时长: X.Xs
✅ 视频可以播放
🎯 创建ScrollTrigger...
✅ ScrollTrigger创建完成
📏 滚动距离: XXXXpx (3个视口高度)
```

**关键日志**：
- ✅ `🔍 [Lenis Detection] Lenis 实例已检测到` - 确认检测到 Lenis
- ✅ `🔗 [Lenis Sync] ✓ 已绑定滚动事件` - 确认同步配置成功
- ✅ `✅ [Lenis Sync] Lenis 同步配置完成` - 确认同步完成

### 步骤 5: 测试滚动播放

1. **向下滚动到视频区域**：
   - 视频应该固定在视口中
   - 视频应该开始播放
   - 调试信息应该显示 "进入区域"

2. **继续向下滚动**：
   - 视频应该跟随滚动进度播放
   - 调试信息应该显示 "播放中"
   - 进度应该从 0% 增加到 100%
   - 时间应该从 0s 增加到视频总时长

3. **向上滚动**：
   - 视频应该逆向播放
   - 进度应该从 100% 减少到 0%
   - 时间应该从总时长减少到 0s

4. **离开视频区域**：
   - 向下离开：调试信息显示 "离开区域"，视频跳到最后一帧
   - 向上离开：调试信息显示 "离开区域"，视频跳到第一帧

### 步骤 6: 验证平滑滚动

- 滚动应该感觉平滑流畅（Lenis 效果）
- 视频播放应该没有跳帧或卡顿
- 滚动和视频播放应该完美同步

## 验证清单

使用以下清单验证所有功能：

### 初始化
- [ ] 控制台显示 Lenis 检测成功
- [ ] 控制台显示 Lenis 同步配置完成
- [ ] 控制台显示视频准备就绪
- [ ] 控制台显示 ScrollTrigger 创建完成
- [ ] 无错误或警告信息

### 视频播放
- [ ] 视频跟随滚动播放
- [ ] 向下滚动 - 视频正向播放
- [ ] 向上滚动 - 视频逆向播放
- [ ] 视频播放流畅，无卡顿
- [ ] 视频和滚动完美同步

### 调试信息
- [ ] 调试信息显示在视频上方
- [ ] 状态信息正确更新
- [ ] 进度百分比正确显示
- [ ] 时间信息正确显示

### 平滑滚动
- [ ] 滚动感觉平滑流畅
- [ ] Lenis 平滑效果明显
- [ ] 无滚动跳跃或抖动

### 固定效果
- [ ] 进入视频区域时，视频固定在视口
- [ ] 滚动时，视频保持固定
- [ ] 离开视频区域时，视频取消固定

### 性能
- [ ] 滚动流畅，无明显延迟
- [ ] CPU 使用率正常
- [ ] 无内存泄漏

## 常见问题排查

### 问题 1: 视频不跟随滚动播放

**可能原因**：
- Lenis 未正确初始化
- Lenis 实例未暴露到 `window.lenis`
- 同步配置失败

**检查**：
1. 控制台是否显示 `🔍 [Lenis Detection] Lenis 实例已检测到`？
2. 控制台是否显示 `✅ [Lenis Sync] Lenis 同步配置完成`？
3. 是否有错误信息？

**解决方案**：
- 确保 `window.lenis = lenis;` 在 Lenis 初始化后执行
- 检查 Lenis 是否正确安装：`npm list lenis`
- 清除缓存并重新启动：`npm run dev`

### 问题 2: 控制台显示 "未检测到 Lenis"

**可能原因**：
- Lenis 脚本加载顺序问题
- Lenis 实例未暴露到全局

**解决方案**：
1. 检查 `window.lenis = lenis;` 是否存在
2. 在控制台手动检查：`console.log(window.lenis)`
3. 确保 Lenis 脚本在 VideoScrollSection 之前加载

### 问题 3: 视频播放卡顿

**可能原因**：
- 重复的 RAF 调用
- 视频文件过大
- 浏览器性能问题

**解决方案**：
1. 确认只有一个 RAF 循环在运行
2. 检查视频文件大小和格式
3. 尝试降低视频质量或使用更小的测试视频

### 问题 4: 滚动不平滑

**可能原因**：
- Lenis 配置问题
- 浏览器兼容性问题

**解决方案**：
1. 检查 Lenis 配置参数
2. 尝试调整 `lerp` 和 `duration` 值
3. 测试不同浏览器

## 对比测试

### 测试 1: 使用 Lenis（test-video-scroll.astro）

访问：`http://localhost:4321/test-video-scroll`

**预期**：
- ✅ 平滑滚动效果
- ✅ 视频跟随滚动播放
- ✅ 控制台显示 Lenis 检测和同步日志

### 测试 2: 不使用 Lenis（pure-test.astro）

访问：`http://localhost:4321/pure-test`

**预期**：
- ✅ 原生滚动
- ✅ 视频跟随滚动播放
- ✅ 控制台显示 "未检测到 Lenis"

### 测试 3: 生产环境（index.astro）

访问：`http://localhost:4321`

**预期**：
- ✅ 完整的页面体验
- ✅ Lenis 平滑滚动
- ✅ VideoScrollSection 正常工作
- ✅ 与其他组件无冲突

## 性能测试

### 使用浏览器性能工具

1. 打开开发者工具 → Performance 标签
2. 点击 Record（录制）
3. 滚动页面 10 秒
4. 停止录制
5. 分析结果

**关键指标**：
- FPS（帧率）：应该保持在 60 FPS
- CPU 使用率：应该低于 50%
- 内存使用：应该稳定，无明显增长

## 自动化测试（未来）

虽然当前是手动测试，但可以考虑添加自动化测试：

```javascript
// 示例：使用 Playwright 进行自动化测试
test('VideoScrollSection 应该跟随滚动播放', async ({ page }) => {
  await page.goto('http://localhost:4321/test-video-scroll');
  
  // 等待视频加载
  await page.waitForSelector('#scroll-video');
  
  // 滚动到视频区域
  await page.evaluate(() => window.scrollTo(0, 1000));
  
  // 检查视频是否播放
  const currentTime = await page.evaluate(() => {
    const video = document.getElementById('scroll-video');
    return video.currentTime;
  });
  
  expect(currentTime).toBeGreaterThan(0);
});
```

## 总结

测试页面提供了一个完整的测试环境，包括：
- ✅ Lenis 平滑滚动集成
- ✅ 详细的调试信息
- ✅ 清晰的测试指南
- ✅ 完整的验证清单

通过这个测试页面，你可以快速验证 VideoScrollSection 组件的所有功能，并确保 Lenis 集成正常工作。
